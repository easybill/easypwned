use std::process::Command;

fn main() {
    println!("build dioxus before running web server");

    // dx build --features web --release --bin easypwned
    // Command::new("dx")
    //     .args(&["build", "--feature", "web", "--release", "--bin", "easypwned"]);
    // Command::new("./tailwind")
}